import { createSlice } from "@reduxjs/toolkit";

const graphSlice = createSlice({
    name:"SessionGraph",
    initialState: {},
    reducers:{
        onNewSession: (state, action) => {

        },

        onTurnOff: (state, action) => {

        }
    }
});